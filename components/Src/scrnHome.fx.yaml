scrnHome As screen:
    OnHidden: =false
    OnVisible: |
        =ClearCollect(
            varMenuData,
            [
                {
                    TabMenuName: "ℹ️ About",
                    GoToTabMenuScreen: scrnAbout
                }
            ]
        );
        Set(
            varSelectedTab,
            {
                TabMenuName: "🏠 Home",
                GoToTabMenuScreen: scrnHome
            }
        );

    cmpntTabMenu_1 As cmpntTabMenu:
        custPropSelectedMenu: =First(Filter(cmpntTabMenu_1.custPropTabLstMenuItems,GoToTabMenuScreen = App.ActiveScreen))
        custPropTabLstMenuItems: =
        Height: =103
        Width: =640

