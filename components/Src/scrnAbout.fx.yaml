scrnAbout As screen:
    OnHidden: =false
    OnVisible: |
        =Set(
            varSelectedTab,
            First(
                Filter(
                    varMenuData,
                    GoToTabMenuScreen = scrnAbout
                )
            )
        );

    cmpntTabMenu_2 As cmpntTabMenu:
        custPropTabLstMenuItems: =varMenuData
        Height: =110
        Width: =640

